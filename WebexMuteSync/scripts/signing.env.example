# Apple Developer signing configuration for build-release.sh
# Copy this file to signing.env and fill in your values:
#   cp signing.env.example signing.env
#
# signing.env is gitignored and will not be committed.

# Your Developer ID Application signing identity (from: security find-identity -v -p codesigning)
SIGN_IDENTITY="Developer ID Application: Your Name (TEAMID)"

# Your Apple Developer Team ID
TEAM_ID="TEAMID"

# Keychain profile for notarytool (created via: xcrun notarytool store-credentials "notarytool")
KEYCHAIN_PROFILE="notarytool"
